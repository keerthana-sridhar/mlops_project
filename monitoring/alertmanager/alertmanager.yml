global:
  resolve_timeout: 5m

route:
  receiver: webhook
  group_wait: 0s
  group_interval: 5s
  repeat_interval: 10s

receivers:
  - name: webhook
    webhook_configs:
      - url: http://backend:8000/internal/alert
        send_resolved: true
